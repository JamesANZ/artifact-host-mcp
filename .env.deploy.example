# Copy to .env.deploy (gitignored) and fill in. Used by: npm run deploy:cloudflare
CLOUDFLARE_API_TOKEN=
# Optional if wrangler picks the wrong cached account (find ID in dashboard URL or wrangler whoami)
# CLOUDFLARE_ACCOUNT_ID=
